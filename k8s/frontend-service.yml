apiVersion: v1
kind: Service
metadata:
  name: frontend-despacho-service
  namespace: apps
spec:
  type: LoadBalancer
  selector:
    app: frontend-despacho
  ports:
    - port: 80
      targetPort: 80
