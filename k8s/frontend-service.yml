apiVersion: v1
kind: Service
metadata:
  name: frontend-despacho-service
  namespace: despachos-app
spec:
  type: LoadBalancer
  selector:
    app: frontend-despacho
  ports:
    - port: 80
      targetPort: 80
